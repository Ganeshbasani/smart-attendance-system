import{useEffect,useState}from'react';import{Database,Download,FileText,LockKeyhole,RefreshCw,ShieldCheck,Users,Settings2}from'lucide-react';import{api}from'../lib/api';import{PageHeader,Stat,Insight,Button,TrustBadge}from'../components/UI';import type{User}from'../App';
export default function Admin({user}:{user:User}){const[health,setHealth]=useState<any>();const[audit,setAudit]=useState<any[]>([]);const[people,setPeople]=useState<any>();const load=()=>Promise.all([api<any>('/admin/system-health'),api<any[]>('/admin/audit'),api<any>('/admin/people')]).then(([h,a,p])=>{setHealth(h);setAudit(a);setPeople(p)});useEffect(()=>{load()},[]);if(!health)return <div className="loading-page">Loading governance console…</div>;return <div><PageHeader eyebrow="Administration" title="Govern the system with evidence." desc="Manage people, inspect audit history, and monitor platform health without operational clutter." action={<button className="btn ghost" onClick={load}><RefreshCw size={15}/>Refresh</button>}/><div className="stats-grid"><Stat label="Students" value={health.students} note="Current roster"/><Stat label="Users" value={health.users} note="RBAC identities"/><Stat label="Attendance records" value={health.attendance_records} note="Durable events"/><Stat label="Audit events" value={health.audit_events} note="Traceable changes"/></div><section id="health" className="two-col"><div className="panel"><div className="section-head"><div><h2>System health</h2><p>Baseline operational checks for the reference deployment.</p></div><Database size={18}/></div><div className="health-list"><div><span>API</span><b className="health-ok">Healthy</b></div><div><span>Database</span><b className="health-ok">Connected</b></div><div><span>RBAC</span><b className="health-ok">Enforced</b></div><div><span>Audit logging</span><b className="health-ok">Enabled</b></div><div><span>Open interventions</span><b>{health.open_interventions}</b></div></div></div><div className="panel"><div className="section-head"><div><h2>Security posture</h2><p>Explicit controls, not decorative badges.</p></div><ShieldCheck size={18}/></div><div className="security-grid"><div><LockKeyhole size={18}/><b>Password hashing</b><span>PBKDF2-HMAC-SHA256</span></div><div><Users size={18}/><b>Role boundaries</b><span>Student · Faculty · HOD · Admin</span></div><div><FileText size={18}/><b>Auditability</b><span>Actor · action · entity · timestamp</span></div><div><Settings2 size={18}/><b>Configuration</b><span>Thresholds and environment variables</span></div></div></div></section><section id="people" className="panel"><div className="section-head"><div><h2>People & roster</h2><p>{people?.users?.length||0} users · {people?.students?.length||0} students</p></div></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Username</th><th>Role</th><th>Status</th></tr></thead><tbody>{people?.users?.map((u:any)=><tr key={u.id}><td>{u.full_name}</td><td>{u.username}</td><td><span className="role-pill">{u.role}</span></td><td>{u.active?'Active':'Disabled'}</td></tr>)}</tbody></table></div></section><section id="audit" className="panel"><div className="section-head"><div><h2>Audit trail</h2><p>Who changed what, when, and on which entity.</p></div><TrustBadge text="Immutable intent"/></div><div className="table-wrap"><table><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{audit.slice(0,50).map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString()}</td><td><b>{a.action}</b></td><td>{a.entity_type} {a.entity_id?'#'+a.entity_id:''}</td><td className="truncate">{a.details||'—'}</td></tr>)}</tbody></table></div></section><section id="policy" className="panel"><div className="section-head"><div><h2>Policy center</h2><p>Attendance thresholds should be configured as institutional policy, not scattered in UI code.</p></div></div><div className="policy-card"><div><span>Default minimum attendance</span><strong>75%</strong></div><div><span>QR token lifetime</span><strong>20 seconds</strong></div><div><span>Risk engine</span><strong>Explainable rules</strong></div><div><span>Human review</span><strong>Required for anomalies</strong></div></div></section></div>}
