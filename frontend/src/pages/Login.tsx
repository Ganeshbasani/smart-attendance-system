import{FormEvent,useState}from'react';import{ShieldCheck,ArrowRight,Activity,LockKeyhole}from'lucide-react';import{authenticate}from'../App';
export default function Login({onAuth}:{onAuth:(u:any)=>void}){const[u,setU]=useState('student');const[p,setP]=useState('AttendX@2026');const[err,setErr]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setErr('');try{const user=await authenticate(u,p);onAuth(user);location.href='/'}catch(e){setErr(e instanceof Error?e.message:'Unable to sign in')}finally{setLoading(false)}}return <div className="login-page"><div className="login-brand"><div className="logo large">A<span>+</span></div><div><strong>AttendX</strong><small>Attendance Intelligence Platform</small></div></div><div className="login-grid"><section className="login-story"><div className="eyebrow">Trust · Intelligence · Early Action</div><h1>Attendance that tells you what to do next.</h1><p>Verify participation, understand risk, predict eligibility, and intervene before attendance becomes a last-minute problem.</p><div className="story-grid"><div><Activity size={18}/><b>Decision-first</b><span>Actionable runway and risk explanations</span></div><div><ShieldCheck size={18}/><b>Trustworthy</b><span>Layered verification and audit trails</span></div><div><LockKeyhole size={18}/><b>Private</b><span>Minimal data collection by default</span></div></div></section><section className="login-card"><div className="eyebrow">Secure access</div><h2>Welcome back</h2><p>Use a demo account to explore the role-specific experience.</p><form onSubmit={submit}><label>Username<input value={u} onChange={e=>setU(e.target.value)} autoComplete="username"/></label><label>Password<input type="password" value={p} onChange={e=>setP(e.target.value)} autoComplete="current-password"/></label>{err&&<div className="form-error">{err}</div>}<button className="btn primary full" disabled={loading}>{loading?'Signing in…':'Sign in'}<ArrowRight size={16}/></button></form><div className="demo-box"><b>Demo accounts</b><span>student · faculty · hod · admin</span><small>Password: AttendX@2026</small></div></section></div><footer>AttendX · Privacy-aware attendance intelligence · Production-oriented reference implementation</footer></div>}
